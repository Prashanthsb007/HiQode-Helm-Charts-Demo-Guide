apiVersion: v1
kind: Service
metadata:
  name: login-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: login-app
  ports:
    - protocol: TCP
      port: {{ .Values.login.port }}
      targetPort: {{ .Values.login.port }}
  type: ClusterIP